<?xml version="1.0" encoding="UTF-8"?>
<!-- sql-test.xml : 마이바티스 실행을 위해 필요한 파일 -->

<!-- mybatis SQL 매핑 파일을 뜻하는 지시문 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 속성에 지정한 패키지명을 가지고 있는 파일과 아래의 xml 구문을 연동한다는 의미 -->
<!-- ★★★ xml 파일이기 때문에 스프링 프로젝트의 전체 구조를 모르기 때문에 전체 패키지 명을 다 입력해야 함 ★★★ -->
<mapper namespace="com.gaya.thegayap.mapper.SinMapper">

    <insert id="insertHotel" parameterType="com.gaya.thegayap.dto.SinDto">
        insert into hotel_table(hotel_name, hotel_addr, hotel_tel, hotel_meal_adult, hotel_meal_child)
        values (#{hotelName}, #{hotelAddr}, #{hotelTel}, #{hotelMealAdult}, #{hotelMealChild});
    </insert>

    <insert id="insertRoom" parameterType="com.gaya.thegayap.dto.SinDto2">



        insert into hotel_room_table(room_hotel_idx, room_name, room_twin_cost, room_twin_weekend, room_code, room_max_adult)
        values (#{roomHotelIdx}, #{roomName}, #{roomTwinCost}, #{roomTwinWeekend},#{roomCode}, #{roomMaxAdult});


    </insert>

    <select id="checkCodeOverlap" parameterType="String" resultType="int">


                select count(*) from hotel_room_table where room_code = #{code};

    </select>


    <select id="checkBookOverlap" parameterType="int" resultType="com.gaya.thegayap.dto.SinRoomDto">
        select room_name,room_twin_cost,room_twin_weekend, room_max_adult, room_code, reservation_check_in,reservation_check_out,reservation_room_code
        from hotel_room_table as hrt
                 left join reservation_table as rt
                           on hrt.room_hotel_num = rt.reservation_hotel_num and hrt.room_code = rt.reservation_room_code
        where hrt.room_hotel_num = 1;
    </select>

    <select id="getHotelNum" parameterType="String" resultType="int">
        select hotel_num from hotel_table
        where hotel_name = "서울가야호텔";
    </select>

    <insert id="reservationRoom" parameterType="com.gaya.thegayap.dto.SinReservDto">
        insert into reservation_table values (#{reservationNum},#{hotelNum},#{roomCode},#{customerName},#{customerId},#{checkIn},#{checkOut},#{reservationTime},#{nights},#{breakfastAdultNum},#{breakfastChildNum},#{reservationPeople},#{totalCost},#{reservationRequest})
    </insert>


</mapper>
